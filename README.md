# deal
Monopoly deal in JS/HTML/CSS

Chat: Socket.IO

# Installation
Run `npm install`

run `node index`

visit localhost

#Vocabulary

deck: stack of cards, generated and in order
stack: shuffled cards